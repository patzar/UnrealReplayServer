@model IEnumerable<UnrealReplayServer.Databases.Models.SessionFile>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartTimeMs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndTimeMs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChunkIndex)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartTimeMs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndTimeMs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChunkIndex)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Filename">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Filename">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Filename">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
